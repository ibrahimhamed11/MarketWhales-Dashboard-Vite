# Force npm to use legacy peer deps resolution
legacy-peer-deps=true

# Use exact versions
save-exact=true

# Disable optional dependencies that might cause issues
optional=false

# Engine strict to false to allow different Node versions
engine-strict=false
